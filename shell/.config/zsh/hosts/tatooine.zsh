# Stationary Mac: private sync is available through the LAN/WireGuard route.
# Atuin reads these overrides on each invocation, including shell history hooks.
export ATUIN_AUTO_SYNC=true
export ATUIN_SYNC_FREQUENCY=5m
